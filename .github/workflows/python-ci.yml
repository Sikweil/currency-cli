name: Python CI

on:
  push:
    branches: [ main, dev ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - run: python -m pip install --upgrade pip
      - run: pip install -e .  # ставит твой пакет
      - run: pip install pytest requests-mock ruff mypy
      - run: ruff check .
      - run: mypy currency_cli       # пропусти, если пока нет type-hints
      - run: pytest -q
